Terraform providers are plugin binaries that allow Terraform to communicate with cloud platforms, SaaS providers, databases, and third-party APIs. Because Terraform is a generic engine, it relies entirely on these providers to translate its declarative .tf configuration files into specific API calls for target platforms. You can discover and download thousands of public plugins directly through the Terraform Registry


